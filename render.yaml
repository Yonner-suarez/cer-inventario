services:
  - type: web
    name: micro-inventario
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PROJECT_PATH
        value: microInventario.API
      - key: PROJECT_DLL
        value: microInventario.API.dll
